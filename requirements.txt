# Pipeline Python dependencies (versions from scienv pip freeze on Alliance HPC).
# Load module gdal/3.9.1 before pip install so GDAL matches the cluster library.
# mpi4py is required for rasterFlowpathEdit.py MPI but may not appear in pip freeze;
# install via pip or the cluster OpenMPI/mpi4py module stack.

GDAL==3.9.1
geopandas==1.0.1
pandas==2.2.3
numpy==1.26.4
scipy==1.15.2
mpi4py==4.0.0
shapely==2.0.7
fiona==1.10.1
pyogrio==0.10.0
pyproj==3.7.1

# Tests
pytest==8.3.4
