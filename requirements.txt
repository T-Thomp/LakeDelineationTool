# Pipeline Python dependencies (pip pins; versions tested with Alliance FIR scienv).
#
# Alliance FIR: before pip install, load modules (do not pip install mpi4py on FIR):
#   module load StdEnv/2023 gdal/3.9.1 mpi4py/4.0.0
# Other sites: load/install GDAL + MPI to match pins below; pip install mpi4py if no module.
# See README.md "Software requirements".

GDAL==3.9.1
geopandas==1.0.1
pandas==2.2.3
numpy==1.26.4
scipy==1.15.2
shapely==2.0.7
fiona==1.10.1
pyogrio==0.10.0
pyproj==3.7.1

# Tests
pytest==8.3.4
